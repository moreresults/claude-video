# claude-video

This is a video processing and skill development project.

## Git Worktrees

**Do NOT create git worktrees for this project.** Never invoke the `using-git-worktrees` skill here. All work runs directly on the main repo at `/Users/jameswatson/Dropbox/GIT/claude-video`.

## Output Files

When the `/watch` skill saves assets with `--save-dir outputs/`, files go to `outputs/` relative to the repo root. Do not resolve paths using the system prompt's working directory display — use the actual repo path.
